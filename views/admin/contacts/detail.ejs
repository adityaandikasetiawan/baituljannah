<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %> - Admin</title>
  <link rel="shortcut icon" href="/assets/img/logo.webp">
  <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
  <link rel="stylesheet" href="/assets/plugins/fontawesome/css/fontawesome.min.css">
  <link rel="stylesheet" href="/assets/plugins/fontawesome/css/all.min.css">
  <link rel="stylesheet" href="/assets/css/iconsax.css">
  <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>
  <div class="main-wrapper">
    <%- include('../../partials/header') %>
    <div class="breadcrumb-bar text-center">
      <div class="container">
        <h2 class="breadcrumb-title mb-2">Detail Kontak</h2>
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb justify-content-center mb-0">
            <li class="breadcrumb-item"><a href="/admin/contacts">Kontak</a></li>
            <li class="breadcrumb-item active">Detail</li>
          </ol>
        </nav>
      </div>
    </div>
    <div class="content">
      <div class="container">
        <div class="row">
          <div class="col-lg-3 theiaStickySidebar">
            <%- include('../../partials/sidebar-admin') %>
          </div>
          <div class="col-lg-9">
            <% if (error) { %><div class="alert alert-danger"><%= error %></div><% } %>
            <% if (success) { %><div class="alert alert-success"><%= success %></div><% } %>
            <div class="card mb-4">
              <div class="card-body">
                <h6 class="mb-3">Detail Pesan</h6>
                <div class="mb-2"><strong>Nama:</strong> <%= contact.name %></div>
                <div class="mb-2"><strong>Email:</strong> <%= contact.email %></div>
                <div class="mb-2"><strong>Subjek:</strong> <%= contact.subject || '-' %></div>
                <div class="mb-2"><strong>Status:</strong> <span class="badge bg-<%= contact.status === 'new' ? 'warning' : contact.status === 'replied' ? 'success' : 'secondary' %>"><%= contact.status %></span></div>
                <div class="mb-2"><strong>Pesan:</strong></div>
                <div class="border rounded p-3"><%= contact.message %></div>
              </div>
            </div>
            <div class="card">
              <div class="card-body">
                <h6 class="mb-3">Balas Pesan</h6>
                <form action="/admin/contacts/<%= contact.id %>/reply" method="post">
                  <div class="mb-3">
                    <textarea name="reply" class="form-control" rows="5" placeholder="Tulis balasan" required><%= contact.reply || '' %></textarea>
                  </div>
                  <button type="submit" class="btn btn-primary">Simpan Balasan</button>
                  <a href="/admin/contacts" class="btn btn-link">Kembali</a>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
 <%- include('../../partials/footer') %>
  </div>
  <script src="/assets/js/jquery-3.7.1.min.js"></script>
  <script src="/assets/js/bootstrap.bundle.min.js"></script>
</body>
</html>