<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %> - Admin</title>
<link rel="shortcut icon" href="/assets/img/logo.webp">
  <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
  <link rel="stylesheet" href="/assets/plugins/fontawesome/css/fontawesome.min.css">
  <link rel="stylesheet" href="/assets/plugins/fontawesome/css/all.min.css">
  <link rel="stylesheet" href="/assets/css/iconsax.css">
  <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>
  <div class="main-wrapper">
    <%- include('../../partials/header') %>
    <div class="breadcrumb-bar text-center">
      <div class="container">
        <h2 class="breadcrumb-title mb-2">Detail Pendaftaran</h2>
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb justify-content-center mb-0">
            <li class="breadcrumb-item"><a href="/admin/registrations">Pendaftaran</a></li>
            <li class="breadcrumb-item active">Detail</li>
          </ol>
        </nav>
      </div>
    </div>
    <div class="content">
      <div class="container">
        <div class="row">
          <div class="col-lg-3 theiaStickySidebar">
            <%- include('../../partials/sidebar-admin') %>
          </div>
          <div class="col-lg-9">
            <% if (error) { %><div class="alert alert-danger"><%= error %></div><% } %>
            <% if (success) { %><div class="alert alert-success"><%= success %></div><% } %>
            <div class="card mb-4">
              <div class="card-body">
                <h6 class="mb-3">Data Pendaftaran</h6>
                <div class="row">
                  <div class="col-md-6">
                    <div class="mb-2"><strong>Nama:</strong> <%= registration.full_name %></div>
                    <div class="mb-2"><strong>Email:</strong> <%= registration.email %></div>
                    <div class="mb-2"><strong>Telepon:</strong> <%= registration.phone || '-' %></div>
                    <div class="mb-2"><strong>Program:</strong> <%= registration.program || '-' %></div>
                  </div>
                  <div class="col-md-6">
                    <div class="mb-2"><strong>Tanggal Daftar:</strong> <%= new Date(registration.created_at).toLocaleString('id-ID') %></div>
                    <div class="mb-2"><strong>Status:</strong> <span class="badge bg-secondary"><%= registration.status %></span></div>
                    <div class="mb-2"><strong>Catatan:</strong> <%= registration.notes || '-' %></div>
                  </div>
                </div>
              </div>
            </div>
            <div class="card">
              <div class="card-body">
                <h6 class="mb-3">Perbarui Status</h6>
                <form action="/admin/registrations/<%= registration.id %>/status" method="post">
                  <div class="row">
                    <div class="col-md-4">
                      <div class="mb-3">
                        <label class="form-label">Status</label>
                        <select name="status" class="form-select">
                          <option value="pending" <%= registration.status === 'pending' ? 'selected' : '' %>>Menunggu</option>
                          <option value="approved" <%= registration.status === 'approved' ? 'selected' : '' %>>Disetujui</option>
                          <option value="rejected" <%= registration.status === 'rejected' ? 'selected' : '' %>>Ditolak</option>
                        </select>
                      </div>
                    </div>
                    <div class="col-md-8">
                      <div class="mb-3">
                        <label class="form-label">Catatan</label>
                        <input type="text" name="notes" class="form-control" value="<%= registration.notes || '' %>">
                      </div>
                    </div>
                  </div>
                  <button type="submit" class="btn btn-primary">Simpan</button>
                  <a href="/admin/registrations" class="btn btn-link">Kembali</a>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
 <%- include('../../partials/footer') %>
  </div>
  <script src="/assets/js/jquery-3.7.1.min.js"></script>
  <script src="/assets/js/bootstrap.bundle.min.js"></script>
</body>
</html>