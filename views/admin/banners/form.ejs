<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %> - Admin</title>
  <link rel="shortcut icon" href="/assets/img/logo.webp">
  <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
  <link rel="stylesheet" href="/assets/plugins/fontawesome/css/fontawesome.min.css">
  <link rel="stylesheet" href="/assets/plugins/fontawesome/css/all.min.css">
  <link rel="stylesheet" href="/assets/css/iconsax.css">
  <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>
  <div class="main-wrapper">
    <%- include('../../partials/header') %>
    <div class="breadcrumb-bar text-center">
      <div class="container">
        <h2 class="breadcrumb-title mb-2"><%= title %></h2>
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb justify-content-center mb-0">
            <li class="breadcrumb-item"><a href="/admin/banners">Banner Beranda</a></li>
            <li class="breadcrumb-item active"><%= banner ? 'Edit' : 'Tambah' %></li>
          </ol>
        </nav>
      </div>
    </div>
    <div class="content">
      <div class="container">
        <div class="row">
          <div class="col-lg-3 theiaStickySidebar">
            <%- include('../../partials/sidebar-admin') %>
          </div>
          <div class="col-lg-9">
            <% if (error) { %><div class="alert alert-danger"><%= error %></div><% } %>
            <div class="card">
              <div class="card-body">
                <form action="/admin/banners<%= banner ? '/' + banner.id : '' %>" method="post" enctype="multipart/form-data">
                  <div class="row">
                    <div class="col-md-6">
                      <div class="mb-3">
                        <label class="form-label">Judul</label>
                        <input type="text" name="title" class="form-control" placeholder="Contoh: Selamat Datang" value="<%= banner ? (banner.title || '') : '' %>">
                      </div>
                    </div>
                    <div class="col-md-6">
                      <div class="mb-3">
                        <label class="form-label">Subjudul</label>
                        <input type="text" name="subtitle" class="form-control" placeholder="Contoh: Madrasah Hebat Bermartabat" value="<%= banner ? (banner.subtitle || '') : '' %>">
                      </div>
                    </div>
                    <div class="col-md-6">
                      <div class="mb-3">
                        <label class="form-label">Link Tombol (opsional)</label>
                        <input type="url" name="link" class="form-control" placeholder="https://..." value="<%= banner ? (banner.link || '') : '' %>">
                      </div>
                    </div>
                    <div class="col-md-3">
                      <div class="mb-3">
                        <label class="form-label">Urutan</label>
                        <input type="number" name="order_index" class="form-control" value="<%= banner ? banner.order_index : 0 %>" min="0">
                      </div>
                    </div>
                    <div class="col-md-3">
                      <div class="mb-3">
                        <label class="form-label">Status</label>
                        <select name="status" class="form-select">
                          <option value="active" <%= banner ? (banner.status==='active' ? 'selected' : '') : 'selected' %>>Aktif</option>
                          <option value="inactive" <%= banner && banner.status==='inactive' ? 'selected' : '' %>>Nonaktif</option>
                        </select>
                      </div>
                    </div>
                    <div class="col-md-12">
                      <div class="mb-3">
                        <label class="form-label">Gambar Banner</label>
                        <% if (banner && banner.image) { %>
                          <div class="mb-2">
                            <img src="<%= banner.image %>" alt="Banner Saat Ini" style="max-height:160px;border-radius:.5rem;object-fit:cover">
                          </div>
                        <% } %>
                        <input type="file" name="image" class="form-control" accept="image/*" <%= banner ? '' : 'required' %>>
                        <small class="text-muted">Disarankan rasio 16:9 atau 21:9, ukuran maksimal 5MB. <%= banner ? 'Kosongkan jika tidak ingin mengganti gambar.' : '' %></small>
                      </div>
                    </div>
                  </div>
                  <button type="submit" class="btn btn-primary"><%= banner ? 'Perbarui' : 'Simpan' %></button>
                  <a href="/admin/banners" class="btn btn-link">Batal</a>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <%- include('../../partials/footer') %>
  </div>
  <script src="/assets/js/jquery-3.7.1.min.js"></script>
  <script src="/assets/js/bootstrap.bundle.min.js"></script>
</body>
</html>