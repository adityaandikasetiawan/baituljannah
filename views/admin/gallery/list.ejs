<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title %> - Admin</title>
  <link rel="shortcut icon" href="/assets/img/logo.webp">
  <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
  <link rel="stylesheet" href="/assets/plugins/fontawesome/css/fontawesome.min.css">
  <link rel="stylesheet" href="/assets/plugins/fontawesome/css/all.min.css">
  <link rel="stylesheet" href="/assets/css/iconsax.css">
  <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>
  <div class="main-wrapper">
    <%- include('../../partials/header') %>
    <div class="breadcrumb-bar text-center">
      <div class="container">
        <h2 class="breadcrumb-title mb-2">Kelola Galeri</h2>
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb justify-content-center mb-0">
            <li class="breadcrumb-item"><a href="/admin/dashboard">Dashboard</a></li>
            <li class="breadcrumb-item active">Galeri</li>
          </ol>
        </nav>
      </div>
    </div>
    <div class="content">
      <div class="container">
        <div class="row">
          <div class="col-lg-3 theiaStickySidebar">
            <%- include('../../partials/sidebar-admin') %>
          </div>
          <div class="col-lg-9">
            <% if (success) { %><div class="alert alert-success"><%= success %></div><% } %>
            <% if (error) { %><div class="alert alert-danger"><%= error %></div><% } %>
            <div class="d-flex justify-content-between align-items-center mb-3">
              <h6 class="mb-0">Daftar Gambar</h6>
              <a class="btn btn-primary" href="/admin/gallery/add"><i class="fa-solid fa-plus me-1"></i>Tambah Gambar</a>
            </div>
            <div class="row">
              <% if (galleries && galleries.length) { %>
                <% galleries.forEach(function(g) { %>
                  <div class="col-md-6 col-lg-4 mb-3">
                    <div class="card h-100">
                      <img src="<%= g.image_path %>" class="card-img-top" alt="<%= g.title %>">
                      <div class="card-body">
                        <h6 class="card-title"><%= g.title %></h6>
                        <p class="card-text small text-muted"><%= g.description || '' %></p>
                        <div class="d-flex justify-content-between align-items-center">
                          <span class="badge bg-secondary"><%= g.category || 'Umum' %></span>
                          <form action="/admin/gallery/<%= g.id %>/delete" method="post" onsubmit="return confirm('Hapus gambar ini?')">
                            <button type="submit" class="btn btn-sm btn-danger">Hapus</button>
                          </form>
                        </div>
                      </div>
                    </div>
                  </div>
                <% }) %>
              <% } else { %>
                <div class="col-12"><p class="text-muted">Belum ada gambar di galeri.</p></div>
              <% } %>
            </div>
          </div>
        </div>
      </div>
    </div>
 <%- include('../../partials/footer') %>
  </div>
  <script src="/assets/js/jquery-3.7.1.min.js"></script>
  <script src="/assets/js/bootstrap.bundle.min.js"></script>
</body>
</html>