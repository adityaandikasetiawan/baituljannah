server {
    listen 80;
    server_name baituljannah.sch.id www.baituljannah.sch.id;

    # Redirect all HTTP to HTTPS (after certificates are installed)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 20M; # allow larger uploads if needed
    }
}

# After SSL is issued by certbot, you can use a HTTPS server block like below:
# server {
#     listen 443 ssl http2;
#     server_name baituljannah.sch.id www.baituljannah.sch.id;
#
#     ssl_certificate /etc/letsencrypt/live/baituljannah.sch.id/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/baituljannah.sch.id/privkey.pem;
#     ssl_trusted_certificate /etc/letsencrypt/live/baituljannah.sch.id/chain.pem;
#
#     # Strong SSL settings (optional, adjust as needed)
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers EECDH+AESGCM:EDH+AESGCM;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         client_max_body_size 20M;
#     }
# }